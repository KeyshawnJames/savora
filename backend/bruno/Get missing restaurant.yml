info:
  name: Get missing restaurant
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/restaurants/nonexistent"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

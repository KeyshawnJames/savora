info:
  name: List restaurants
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/restaurants"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

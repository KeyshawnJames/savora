# Base URL of the Savora backend API.
# Vite only exposes vars prefixed with VITE_ to browser code.
# Copy this file to .env and adjust if your backend runs elsewhere.
VITE_API_URL=http://localhost:3000
